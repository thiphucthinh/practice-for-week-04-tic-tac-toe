const Cursor = require("./cursor.js");
const Screen = require("./screen.js");

class TTT { // TTT is responsible for game play
    constructor(playerTurn, grid, cursor) {
        this.playerTurn = 0;
        this.grid;
        this.cursor = new Cursor;
    }

    // Screen.initialized();
    // Screen.gridLines();
    // Screen.message();
    // Screen.backgroundColors();

}


module.exports = TTT;
